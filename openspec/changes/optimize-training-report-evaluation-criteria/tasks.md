## 1. 规范与配置冻结

- [ ] 1.1 建立指标字典，定义主评分指标、观察指标、单位、取值范围、方向和数据来源。
- [ ] 1.2 建立 `RubricConfig` 初版，包含阈值、权重、`AllowedAxes`、平滑参数、防抖参数和稳定窗口参数。
- [ ] 1.3 建立日志字段规范，定义事件流、位姿流、派生指标、门槛结果和版本字段。

## 2. 会话采集实现

- [ ] 2.1 新增 `SessionRecorder`，记录步骤开始、步骤结束、重试、强制结束和异常流程事件。
- [ ] 2.2 接入 Marker/Scalpel/Tracheal 尖端位姿采样，并支持 20-50Hz 可配置采样率。
- [ ] 2.3 将采样窗口绑定到当前激活步骤，避免非训练步骤产生冗余轨迹。
- [ ] 2.4 实现短时误触防抖，确保低于阈值的接触不计为有效操作证据。
- [ ] 2.5 实现稳定窗口确认，确保连续满足设定帧数后才确认切开或插管有效。

## 3. 指标与评分引擎

- [ ] 3.1 实现区域局部坐标系下的定位角度误差、中点偏移和标记长度计算。
- [ ] 3.2 实现切开角度误差、切口长度、中心偏移和路径效率计算。
- [ ] 3.3 实现插管成功状态、稳定停留时长和插管角度误差计算。
- [ ] 3.4 实现 `AllowedAxes` 横切、竖切、双路径方向误差计算。
- [ ] 3.5 实现 `ScoreEngine` 的指标标准化、权重汇总和确定性复算。
- [ ] 3.6 实现门槛判定，覆盖跳步、逆序、关键步骤未完成和危险事件超阈值。
- [ ] 3.7 将时间和重试次数接入观察指标，确保主评分权重固定为 0。

## 4. 报告与导出

- [ ] 4.1 重构 `TrainingReportManager`，将报告展示拆分为主评分区、门槛状态区、观察指标区和改进建议区。
- [ ] 4.2 新增 `ResearchExporter` CSV 导出，输出原始指标、标准化指标、门槛状态和版本字段。
- [ ] 4.3 新增 `ResearchExporter` JSON 导出，保留会话、步骤、事件、采样和派生指标的结构化层级。
- [ ] 4.4 输出字段映射说明，确保 R、Python 或 SPSS 可直接读取导出数据。
- [ ] 4.5 在导出记录中加入 `algorithm_version`、`rubric_version` 和 `scenario_version`。

## 5. 验证与论文材料

- [ ] 5.1 建立 6 组基准回放用例，覆盖理想、横切合规、竖切合规、偏移错误、危险事件和未完成场景。
- [ ] 5.2 为每组基准回放定义预期分数区间、门槛结果和关键指标范围。
- [ ] 5.3 跑通自动回放验证，并生成系统验证报告。
- [ ] 5.4 完成同一操作者多轮重复训练趋势展示，呈现关键误差指标和主评分变化。
- [ ] 5.5 生成论文方法学模板，描述系统架构、数据采集、评分公式和门槛规则。
- [ ] 5.6 生成可复现附录模板，包含字段字典、参数版本、示例日志和复算说明。
